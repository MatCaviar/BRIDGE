{
  "name": "@im/mcp-pipeline-cli",
  "version": "0.1.24",
  "description": "Host-side MCP projection and real-device invocation CLI for BRIDGE",
  "type": "module",
  "engines": {
    "node": ">=20.11",
    "npm": ">=10"
  },
  "bin": {
    "mcp-pipeline": "./bin/mcp-pipeline.js"
  },
  "scripts": {
    "build": "tsc",
    "check-manifests": "node ../scripts/check-manifests.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "zod": "^3.24.0"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.6",
    "vitest": "^3.0.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
