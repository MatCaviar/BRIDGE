<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Target package queries and binding permissions belong in the integrator's manifest overlay. -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <application android:label="BRIDGE Executor" android:allowBackup="false">
        <activity android:name=".ExecutorActivity" android:exported="true" android:theme="@android:style/Theme.DeviceDefault.NoActionBar" />
    </application>
</manifest>
