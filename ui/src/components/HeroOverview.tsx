import { useWorkbench } from "../state/workbench";
export function HeroOverview() { const { connection, project, artifacts, restoreSession, setRestoreSession } = useWorkbench(); return <header className="hero glass"><div><p className="eyebrow">BRIDGE 控制面</p><h1>BRIDGE 可视化工作台</h1><p className="hero-copy">从源码接口发现，到 Curate、MCP 投影、RPC 接线与真实调用，一条可审计的转换轨道。</p></div><div className="telemetry"><span className={`status ${connection}`}>{connection === "online" ? "本地控制面在线" : connection === "checking" ? "正在连接本地控制面" : "本地控制面离线"}</span><strong>{project?.name ?? "等待导入项目"}</strong><small>{artifacts ? `${artifacts.coverage.wired}/${artifacts.coverage.discovered} 已接线` : "尚无分析产物"}</small><label className="session-toggle"><input type="checkbox" checked={restoreSession} onChange={(event) => setRestoreSession(event.target.checked)} /><span>打开时恢复上次会话</span></label></div></header>; }
